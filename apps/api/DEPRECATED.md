# Deprecated

This FastAPI backend is **no longer used**. Safar v3 operates entirely on **Supabase** (Auth, PostgreSQL, Storage, Realtime, RLS).

Use `apps/web` with the service layer in `src/services/supabase/`.

Do not deploy or run this API for new installations.
